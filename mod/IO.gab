io = 'cio'.use

println:.def () => io.stdout.write('$\n'.sprintf(self))

printf:.def(
  Strings.t
  (args*) => do
    io.stdout.write(self.sprintf(args*))
  end)

#Socket = io.Sock
#File = io.File

io
