nums := [1 2 3 4 5 6 7 8 9]

xf := Transducers.deduce([] (xs x) :: do
      xs := xs .cons x
      done := xs.len > 4
      '$ $ $\n'.printf(done xs x)
      (done, xs)
    end)

new_nums := nums.collect([] xf)

'$\n' .printf new_nums
